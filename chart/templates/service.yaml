apiVersion: v1
kind: Service
metadata:
  name: token-display
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: token-display
  ports:
    - name: http
      port: 8080
      targetPort: 8080
